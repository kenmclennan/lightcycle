"""Domain: the typed, IO-free model (entities, value objects, aggregates).

Being migrated from the functional `core/` modules per Phase 4 of GRID-013 -
one module at a time, behaviour-preserving, with the suite as the net.
"""
